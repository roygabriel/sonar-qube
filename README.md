# sonar-qube
Replicate and adapt the MCP server implementation from cruvero/cruvero-mcp-k8s@dev, specifically main.go, pkg/server/handlers.go, pkg/server/config.go, and deployment/helm/values.yaml, templates/deployment.yaml into the current environment while preserving original behavior.
