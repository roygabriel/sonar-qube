# Acceptance Criteria

| ID | Measurable Outcome | Edge Cases | Validation Command | Evidence | Status | Notes |
|---|---|---|---|---|---|---|
| AC-01 | Core server functionality from main.go and pkg/server/ matches reference behavior on happy path with full functional and output parity | Invalid configs, missing env vars, K8s API timeouts, high concurrency, unsupported versions, resource exhaustion | `go test ./pkg/server/... -race -coverprofile=coverage.out && go tool cover -func=coverage.out` | Test output (all PASS), coverage report (>=90%), benchmark results (<50ms avg), reference behavior diff logs | Pending | Assigned to: GoSpecialist. Priority: 1 (phase-1, risk:0.1) |
| AC-02 | Helm deployment from deployment/helm/ succeeds in target cluster with no errors | Cluster connectivity failures, chart syntax errors, RBAC/permission issues, namespace conflicts, K8s version incompatibility, insufficient resources | `helm lint ./deployment/helm && helm template ./deployment/helm \| kubectl apply --dry-run=server -f - && helm install --dry-run test-release ./deployment/helm` | Helm lint/template output (no errors), kubectl dry-run success logs, pod status confirmation (all Running), events log with zero errors | Pending | Assigned to: K8sEngineer. Priority: 2 (phase-2, risk:0.2) |
| AC-03 | Dedicated testing phase completes with all tests passing and zero critical defects | Boundary inputs, negative error paths, load thresholds, concurrent test execution, timeout scenarios, dependency injection failures | `go test ./... -race -coverprofile=coverage.out && go tool cover -func=coverage.out \| grep -E 'coverage: [8-9][0-9]'` | Full test report (100% pass rate), coverage report (>=85%), defect log (zero critical), junit xml results, benchmark suite output | Pending | Assigned to: TestEngineer. Priority: 3 (phase-3, risk:0.1) |
| AC-04 | Failure handling and rollback function as verified during rollout | Pod crashes, network partitions, config update failures, partial rollout errors, API rate limits, statefulset migration issues | `kubectl rollout status deployment/mcp-k8s-server --timeout=60s && ./scripts/test-rollback.sh && kubectl rollout undo deployment/mcp-k8s-server --to-revision=0` | Rollout status logs (success), rollback verification output, monitoring metrics (zero downtime), event logs confirming recovery, before/after pod states | Pending | Assigned to: K8sEngineer. Priority: 4 (phase-4, risk:0.1) |
| AC-05 | All phase exit criteria and binary test results are met | Partial phase completion, dependency chain breaks, timeout on exit checks, cross-phase data inconsistencies, rollback impact on final state | `./verify-all-phases.sh && go test -run=TestExitCriteria -race && grep -q "All phases: PASS" phase-report.log` | Phase checklist (all signed off), binary test results (PASS), consolidated coverage report, final benchmark summary, signed verification document | Pending | Assigned to: MCPArchitect. Priority: 5 (phase-5, risk:0.1) |